[tools]
python = "3.11"

[tasks.setup]
description = "Install dependencies for local development"
run = "uv sync --extra dev"

[tasks.tool]
description = "Run the TUI for experimentation"
run = "uv run shmample"

[tasks.test]
description = "Run the test suite"
run = "uv run pytest"
